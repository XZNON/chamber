inputc= ['create a frontend for my shoe selling store using react'],
goals = [Goal(description='Create the project folder structure for the React frontend application'), Goal(description='Create a root directory for the React frontend application.'), Goal(description='Set up a main application component file as the entry point of the frontend.'), Goal(description='Define a layout component to wrap the main content and handle global UI elements.'), Goal(description='Implement a routing component to manage navigation between different views.'), Goal(description='Create a base CSS file defining global styles and variables for the application.'), Goal(description='Build a header UI component with basic navigation links.'), Goal(description='Build a footer UI component to appear on all pages.'), Goal(description='Define the data schema for the Shoe entity with fields for name, image URL, price, and featured flag.'), Goal(description='Define the data schema for the Promotion entity with fields for title, description, image URL, and active status.'), Goal(description='Implement the backend logic to query and return a list of featured shoes based on the featured flag.'), Goal(description='Implement the backend logic to query and return active promotions.'), Goal(description='Configure the API route to handle GET requests for fetching featured shoes.'), Goal(description='Configure the API route to handle GET requests for fetching active promotions.'), Goal(description='Create the homepage UI component structure with sections for featured shoes and promotions.'), Goal(description='Implement the frontend logic to fetch featured shoes from the API and display them in the featured shoes section.'), Goal(description='Implement the frontend logic to fetch active promotions from the API and display them in the promotions section.'), Goal(description='Define the CSS styles for the homepage layout with distinct styling for featured shoes and promotions sections.'), Goal(description='Define the data schema for the Shoe product entity including fields for name, brand, price, size, color, and category.'), Goal(description='Create the backend service file responsible for retrieving shoe product data from the database.'), Goal(description='Implement server-side logic to filter shoe products by attributes such as size, color, and category.'), Goal(description='Implement server-side logic to sort shoe products by price and popularity.'), Goal(description='Configure the API route to handle GET requests to fetch filtered and sorted shoe product listings.'), Goal(description='Create the frontend page component to display the list of shoe products with their details shown.'), Goal(description='Build the filtering UI component with controls for selecting shoe size, color, and category.'), Goal(description='Build the sorting UI component with controls for selecting sorting criteria like price and popularity.'), Goal(description='Integrate the filtering and sorting UI controls with the API to dynamically update the product listings.'), Goal(description='Define CSS styles for the product listing page, including layout for product cards and filter/sort controls.'), Goal(description='Develop product detail pages with images, descriptions, prices, and purchase options'), Goal(description='Implement a shopping cart component to add, remove, and view selected shoes'), Goal(description='Create a user authentication interface for login and registration'), Goal(description='Develop a checkout process interface for users to place orders'), Goal(description='Add responsive styling and UI polish to ensure a visually appealing and user-friendly experience'), Goal(description='Test the frontend application for usability and functionality')]